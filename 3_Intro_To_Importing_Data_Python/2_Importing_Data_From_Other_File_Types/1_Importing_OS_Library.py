# The Python Library 'os' contains miscellaneous operating system interfaces

import os           # Importing the library os
wd = os.getcwd()    # Storing the name of the current directory in a string called wd
os.listdir(wd)      # Outputs the contents of the directory in a list to the shell